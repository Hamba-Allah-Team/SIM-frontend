// import { contentsDummy } from "../../dataDummy"
// import { EditContentForm } from "./formEditContent"

// type Params = { params: { id: string } }

// export default function EditContentPage({ params }: Params) {
//   const id = Number(params.id)

//   // Cari data dari dummy berdasarkan id
//   const contentData = contentsDummy.find(item => item.contents_id === id)

//   if (!contentData) return <p>Konten tidak ditemukan</p>

//   return <EditContentForm initialData={contentData} />
// }
